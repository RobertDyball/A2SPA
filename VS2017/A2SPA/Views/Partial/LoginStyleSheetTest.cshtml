
@*  this is a server side comment, invisible to client *@

@*  the following could be emitted from C# or razor code, or plain text, or both *@

@{
    ViewData["myColor"] = "red";
}

p.styleLinkedFromPartialController {
    color:@ViewData["myColor"]
}